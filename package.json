{
  "name": "appointment-processor-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api-server",
    "worker"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:api": "npm run dev --workspace=api-server",
    "dev:worker": "npm run dev --workspace=worker"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "groq-sdk": "^0.33.0",
    "tesseract.js": "^6.0.1"
  }
}
